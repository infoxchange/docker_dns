language: python
python:
  - "2.7"
install:
  - pip install -q -r requirements.txt --use-mirrors
  - pip install -q fudge==1.0.3 pep8==1.4.6 pylint==1.0.0
script:
  - pep8 *.py
  - pylint --rcfile=pylint.conf *.py
  - ./docker_dns_test.py